// Provider deployment contract for the polyrepo reusable-deploy pipeline.
//
// Shape mirrors ProviderDeploymentConfigSchema in the monorepo-owned provider
// registry package. This split repo intentionally keeps the deploy contract as a
// plain object so the central deploy engine can validate it without making the
// private registry package a runtime dependency here.
//
// Swing Taxi is a plain HTTP provider behind the vendor's IP allowlist. It must
// run with direct pod egress (no proxy pool, no apifuse.io/egress-class label)
// so all traffic leaves through the cluster NAT stable EIP that Swing
// allowlists — see the header comment in index.ts.

type ProviderDeploymentConfig = {
  runtime: "shared" | "dedicated" | "browser";
  language: "typescript" | "python";
  replicas?: number;
  hpa?: {
    enabled: boolean;
    minReplicas?: number;
    maxReplicas?: number;
    targetCPUUtilizationPercentage?: number;
  };
  resources: {
    cpu: string;
    memory: string;
  };
  cache?: {
    redis?: {
      enabled: boolean;
      url?: string;
    };
  };
  network?: {
    additionalTcpPorts?: number[];
  };
};

const deployment = {
  runtime: "shared",
  language: "typescript",
  replicas: 1,
  hpa: {
    enabled: true,
    minReplicas: 1,
    maxReplicas: 2,
    targetCPUUtilizationPercentage: 70,
  },
  resources: {
    cpu: "300m",
    memory: "512Mi",
  },
} satisfies ProviderDeploymentConfig;

export default deployment;

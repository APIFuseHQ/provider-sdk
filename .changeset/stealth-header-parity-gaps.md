---
"@apifuse/provider-sdk": major
---

Close measured Chrome parity gaps in the stealth transport: cookie and extension-header ordering, Range-specific `Accept-Encoding`, explicit request-class declarations, and HTTP/1.1 casing, `Host`/`Connection` ordering, and `priority` suppression.

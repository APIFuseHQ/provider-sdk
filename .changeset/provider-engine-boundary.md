---
"@apifuse/provider-sdk": major
---

Add the provider engine attachment and protocol boundary, explicit vanilla versus engine-resident runtime targets, runtime fail-closed capability injection, and engine-owned proxy credential containment. Provider proxy declarations now express policy only and may not declare platform proxy-vendor credentials.

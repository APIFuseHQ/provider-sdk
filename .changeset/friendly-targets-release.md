---
"@apifuse/provider-sdk": minor
---

Fix pooled browser policy teardown so disposed CDP targets are never reconnected, and add request-local HTTP cancellation through retries and native response-body consumption.

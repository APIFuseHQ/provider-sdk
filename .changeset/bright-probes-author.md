---
"@apifuse/provider-sdk": minor
---

Allow operation health-check cases to own declarative health scenarios with exclusive imperative hooks and operation-scoped coverage.

This is a minor release because it adds an optional, backward-compatible provider authoring surface.
